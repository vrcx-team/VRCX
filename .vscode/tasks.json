{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "build-all",
            "command": "${workspaceFolder}/build-scripts/build-all.ps1",
            "args": ["-NoCI"],
            "options": { "cwd": "${workspaceFolder}/build-scripts" },
            "problemMatcher": []
        },
        {
            "type": "shell",
            "label": "clean",
            "command": "Remove-Item",
            "args": ["-Path", "${workspaceFolder}/build/*", "-Recurse"],
            "problemMatcher": []
        }
    ]
}

