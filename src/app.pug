doctype html
#x-app.x-app(@dragenter.prevent @dragover.prevent @drop.prevent)
    //- ### Login ###
    LoginPage(v-if="!this.store.auth.isLoggedIn")

    VRCXUpdateDialog

    template(v-if='this.store.auth.isLoggedIn')
        //- ### Menu ###
        NavMenu(ref='menu')

        //- ### Sidebar ###
        Sidebar

        //- ### Tabs ###
        FeedTab

        GameLogTab

        PlayerListTab

        SearchTab

        FavoritesTab

        FriendLogTab

        ModerationTab

        NotificationTab

        FriendListTab

        ChartsTab

        ProfileTab

        SettingsTab

        //- ## Dialogs ## -\\
        PreviousInstancesInfoDialog

        FriendImportDialog

        WorldImportDialog

        AvatarImportDialog

        ChooseFavoriteGroupDialog

        LaunchDialog

        UserDialog

        WorldDialog

        GroupDialog

        AvatarDialog

        FullscreenImageDialog

        LaunchOptionsDialog

        EditInviteMessageDialog

        VRChatConfigDialog

        PrimaryPasswordDialog

        GalleryDialog
