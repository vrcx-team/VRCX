import {
    checkVRChatCache,
    deleteVRChatCache,
    displayLocation,
    extractFileId,
    extractFileVersion,
    extractVariantVersion,
    getAvailablePlatforms,
    getBundleLocation,
    getLaunchURL,
    getPrintFileName,
    getPrintLocalDate,
    isFriendOnline,
    isRealInstance,
    parseLocation
} from './index';

const utils = {
    getAvailablePlatforms,
    deleteVRChatCache,
    checkVRChatCache,
    getLaunchURL,
    extractFileId,
    extractFileVersion,
    extractVariantVersion,
    isRealInstance,
    displayLocation,
    parseLocation,
    getPrintFileName,
    getPrintLocalDate,
    isFriendOnline,
    getBundleLocation
};

export { utils };
